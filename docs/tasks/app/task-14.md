# Task 14

## Goal
반려동물 전환 상태를 홈/도메인 화면에서 공유할 수 있는 최소 구조를 만든다.

## Files
- app/src/screens/pets/pet-switcher-sheet.tsx
- app/src/screens/home/home-screen.tsx

## Requirements
- selected pet id를 상위 화면에서 소유하는 구조를 정리한다.
- `PetSwitcherSheet` 선택 이벤트가 상위 상태 변경으로 이어지게 한다.

## Constraints
- 선행 task: task-11.md
- 전역 상태 라이브러리 도입은 하지 않는다.

## Done Criteria
- 반려동물 전환 UI가 실제 상태 변경 지점과 연결될 수 있다.

