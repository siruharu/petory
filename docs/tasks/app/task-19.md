# Task 19

## Goal
일정 완료 액션을 목록 또는 카드에서 호출할 수 있는 구조를 만든다.

## Files
- app/src/screens/schedules/schedule-list-screen.tsx
- app/src/components/cards/schedule-card.tsx
- app/src/features/schedules/schedule-api.ts

## Requirements
- complete 버튼 또는 액션 자리를 추가한다.
- `completeSchedule()` 호출 구조를 연결한다.
- 완료 후 재조회 또는 상태 갱신 자리를 둔다.

## Constraints
- 선행 task: task-17.md
- 실제 낙관적 업데이트는 포함하지 않는다.

## Done Criteria
- 일정 완료 흐름이 UI에서 호출 가능해진다.

