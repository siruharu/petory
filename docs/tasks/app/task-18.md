# Task 18

## Goal
일정 생성 폼 화면을 실제 submit 가능한 상태로 연결한다.

## Files
- app/src/screens/schedules/schedule-form-screen.tsx
- app/src/components/forms/schedule-form.tsx

## Requirements
- local state로 schedule payload를 구성한다.
- submit 시 `createSchedule()` 호출 구조를 만든다.
- 폼 props를 실제로 사용한다.

## Constraints
- 선행 task: task-17.md
- 달력 UI는 도입하지 않는다.

## Done Criteria
- 일정 생성 화면이 실제 생성 요청을 보낼 수 있다.

