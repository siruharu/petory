# Task 03

## Goal
`AuthProvider`와 `RootNavigator`를 실제 앱 루트에서 조립한다.

## Files
- app/App.tsx
- app/src/app/providers/auth-provider.tsx
- app/src/app/navigation/root-navigator.tsx

## Requirements
- `App.tsx`에서 `AuthProvider`를 사용한다.
- `RootNavigator`가 앱 루트에서 렌더되게 한다.
- 최소한 auth 상태 분기와 app 상태 분기를 유지한다.

## Constraints
- 선행 task: task-02.md
- 실제 도메인 화면 연결은 최소 수준으로 둔다.

## Done Criteria
- 앱 시작 시 provider와 root navigator가 실제 렌더 트리에 올라간다.

