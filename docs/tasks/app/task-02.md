# Task 02

## Goal
모바일 앱 엔트리 파일을 추가해 루트 렌더 트리를 시작할 수 있게 한다.

## Files
- app/App.tsx
- app/index.js

## Requirements
- `App.tsx`에서 루트 컴포넌트를 export 한다.
- `index.js`에서 앱 등록 엔트리를 추가한다.
- 현재 존재하는 `AuthProvider`와 `RootNavigator`를 조립할 자리를 만든다.

## Constraints
- 선행 task: task-01.md
- 실제 네비게이션 라이브러리 연결은 이 task에서 끝내지 않아도 된다.

## Done Criteria
- 앱 엔트리 파일이 존재한다.
- React Native 런타임이 앱 루트를 찾을 수 있는 구조가 된다.

