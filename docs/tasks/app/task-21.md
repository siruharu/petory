# Task 21

## Goal
기록 생성 폼 화면을 실제 submit 가능한 상태로 연결한다.

## Files
- app/src/screens/records/record-form-screen.tsx
- app/src/components/forms/record-form.tsx

## Requirements
- local state로 record payload를 구성한다.
- submit 시 `createRecord()` 호출 구조를 만든다.
- 병원 방문 메모형 기록 입력을 유지한다.

## Constraints
- 선행 task: task-20.md
- 상세 validation은 최소 수준으로 둔다.

## Done Criteria
- 기록 생성 화면이 실제 생성 요청을 보낼 수 있다.

