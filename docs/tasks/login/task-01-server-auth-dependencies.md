# Task 01

## Goal
로그인 기능에 필요한 서버 의존성 골격을 추가한다.

## Files
- server/build.gradle.kts

## Requirements
- JPA 의존성을 추가한다.
- H2 의존성을 추가한다.
- JWT 라이브러리 의존성을 추가한다.
- 메일 발송 의존성을 추가한다.

## Constraints
- 기준 문서: `docs/plan-login.md`
- 아직 실제 인증 로직은 구현하지 않는다.
- 기술 선택은 `JPA + H2`를 유지한다.

## Done Criteria
- 서버 빌드 파일에 로그인 구현에 필요한 핵심 의존성이 선언된다.

