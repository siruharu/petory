# Task 02

## Goal
로그인/이메일 인증 관련 `application.yml` 설정 구조를 추가한다.

## Files
- server/src/main/resources/application.yml

## Requirements
- JWT 설정 키 구조를 추가한다.
- H2 datasource 설정 구조를 추가한다.
- 메일 발송 설정 구조를 추가한다.
- CORS allowed origins 설정 구조를 추가한다.

## Constraints
- 선행 task: `task-01-server-auth-dependencies.md`
- env placeholder 중심으로 작성한다.
- 실제 비밀값을 하드코딩하지 않는다.

## Done Criteria
- 서버 설정 파일에서 auth/db/mail/cors 키 구조를 확인할 수 있다.

