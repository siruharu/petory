# Task 08

## Goal
JWT/이메일 인증 설정 프로퍼티 클래스를 추가한다.

## Files
- server/src/main/kotlin/com/petory/auth/AuthProperties.kt

## Requirements
- JWT issuer, secret, expiration 설정을 바인딩한다.
- verification token expiration, verify base URL 설정을 바인딩한다.

## Constraints
- 선행 task: `task-02-auth-application-config.md`
- 설정 바인딩 클래스만 작성한다.

## Done Criteria
- auth 설정을 코드에서 안전하게 참조할 수 있는 프로퍼티 클래스가 생긴다.

