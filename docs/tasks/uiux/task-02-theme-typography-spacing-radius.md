# Task 02: Theme Typography Spacing Radius

## Goal

타이포, spacing, radius 토큰을 추가한다.

## Files

- `app/src/theme/typography.ts`
- `app/src/theme/spacing.ts`
- `app/src/theme/radius.ts`
- `app/src/theme/index.ts`

## Requirements

- display, title, body, label 계층 정의
- spacing scale 정의
- radius scale 정의
- index에서 재export

## Constraints

- 폰트 패밀리 외부 의존성은 추가하지 않는다
- 컴포넌트 적용까지 한 task에 넣지 않는다

## Done Criteria

- typography, spacing, radius 토큰 파일이 생성된다
- 값이 역할 기반 이름으로 정리된다

